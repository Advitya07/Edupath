import { AlertTriangle, CheckCircle2, Target } from 'lucide-react'
export default function QuizResult({ result, onContinue }) { const gaps = Object.entries(result.topic_scores).filter(([,score]) => score < 55); return <section className="mx-auto max-w-2xl"><div className="card overflow-hidden"><div className="bg-gradient-to-r from-sky-500/20 to-indigo-500/20 p-7 text-center"><Target className="mx-auto text-sky-300" size={32}/><p className="mt-3 text-sm text-slate-400">Calibrated mastery</p><p className="text-5xl font-bold">{result.overall}%</p></div><div className="space-y-3 p-6">{Object.entries(result.topic_scores).map(([topic,score]) => <div key={topic} className="flex items-center gap-3"><span className={score < 55 ? 'text-amber-300' : 'text-emerald-300'}>{score < 55 ? <AlertTriangle size={18}/> : <CheckCircle2 size={18}/>}</span><span className="w-28 text-sm">{topic}</span><div className="h-2 flex-1 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-sky-400" style={{width:`${score}%`}}/></div><b className="text-sm">{score}%</b></div>)}<p className="rounded-xl bg-slate-950/50 p-3 text-sm text-slate-400">{gaps.length ? `Your next leverage point: ${gaps.map(([t]) => t).join(', ')}. The path will prioritize it.` : 'Strong calibration. Your roadmap is ready for deeper practice.'}</p><button onClick={onContinue} className="btn-primary w-full">Open my learning path</button></div></div></section> }
