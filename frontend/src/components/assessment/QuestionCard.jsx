import RatingButtons from './RatingButtons'
export default function QuestionCard({ question, index, answer, onAnswer }) { return <article className="card p-5"><p className="eyebrow">Question {index + 1} · {question.topic}</p><h2 className="mt-2 text-lg font-semibold">{question.prompt}</h2><div className="mt-5 grid gap-2">{question.options.map((option, i) => <button key={option} onClick={() => onAnswer({ ...answer, selected_index: i })} className={`rounded-xl border p-3 text-left text-sm transition ${answer?.selected_index === i ? 'border-sky-300 bg-sky-400/10 text-sky-100' : 'border-line bg-slate-950/30 text-slate-400 hover:border-slate-500'}`}><span className="mr-3 inline-grid h-5 w-5 place-items-center rounded-full border border-current text-xs">{String.fromCharCode(65+i)}</span>{option}</button>)}</div><div className="mt-5 border-t border-line pt-4"><p className="mb-2 text-xs font-medium text-slate-400">How confident are you?</p><RatingButtons value={answer?.confidence} onChange={confidence => onAnswer({ ...answer, confidence })}/></div></article> }
