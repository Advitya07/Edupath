import { Check, FileText, Sparkles, Upload } from 'lucide-react'
import { useRef, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { parseResume, saveProfile } from '../../services/authApi'
import { useAuthStore } from '../../store/authStore'
const tracks=['Full Stack Engineer','ML Engineer','DevOps','Data Scientist','Cloud Architect']
export default function OnboardingWizard(){const user=useAuthStore(s=>s.user),updateUser=useAuthStore(s=>s.updateUser),nav=useNavigate(),fileRef=useRef();const [career,setCareer]=useState('Full Stack Engineer'),[experience,setExperience]=useState('Early career'),[resume,setResume]=useState(null),[parsed,setParsed]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const upload=async file=>{if(!file)return;setResume(file);setError('');try{setParsed(await parseResume(file))}catch(e){setError(e.response?.data?.detail||'Could not parse that file')}};const continuePath=async()=>{setBusy(true);try{const profile=await saveProfile(user.id,{career_target:career,experience_level:experience,resume_text:''});updateUser({...user,career_target:career,skills:parsed?.skills||profile.skills||[]});nav('/assessment')}catch{updateUser({...user,career_target:career,skills:parsed?.skills||[]});nav('/assessment')}finally{setBusy(false)}};return <main className="min-h-screen bg-[radial-gradient(circle_at_top,_#172c4d,_#07111f_48%)] p-5"><div className="mx-auto max-w-3xl py-8"><div className="flex items-center gap-2 text-sky-300"><Sparkles size={18}/><b>EduPath setup</b></div><div className="mt-7"><p className="eyebrow">Step 1 of 2</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">Tell us where you’re headed.</h1><p className="mt-3 text-slate-400">We’ll compare your current signal with the skills your target role needs.</p></div><section className="card mt-8 p-5 sm:p-7"><label className="text-sm font-semibold">Career destination</label><div className="mt-3 grid gap-2 sm:grid-cols-2">{tracks.map(track=><button key={track} onClick={()=>setCareer(track)} className={`rounded-xl border p-4 text-left text-sm font-semibold ${career===track?'border-sky-300 bg-sky-400/10 text-sky-200':'border-line text-slate-400 hover:border-slate-500'}`}>{career===track&&<Check className="mb-2 text-sky-300" size={16}/>} {track}</button>)}</div><label className="mt-6 block text-sm font-semibold">Experience level</label><select className="field mt-2" value={experience} onChange={e=>setExperience(e.target.value)}><option>Early career</option><option>1–3 years</option><option>3–5 years</option><option>5+ years</option></select></section><section className="card mt-4 p-5 sm:p-7"><div className="flex items-start gap-3"><FileText className="mt-1 text-indigo-300"/><div><h2 className="font-semibold">Make it sharper with your résumé</h2><p className="mt-1 text-sm text-slate-400">Optional · PDF or TXT · your file is parsed and not retained.</p></div></div><button onClick={()=>fileRef.current.click()} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border border-dashed border-sky-400/50 bg-sky-400/5 px-4 py-6 text-sm font-semibold text-sky-200 hover:bg-sky-400/10"><Upload size={18}/>{resume?resume.name:'Upload résumé'}</button><input ref={fileRef} className="hidden" type="file" accept=".pdf,.txt" onChange={e=>upload(e.target.files?.[0])}/>{parsed&&<div className="mt-4 rounded-xl bg-emerald-400/10 p-4 text-sm"><p className="font-semibold text-emerald-300">Found {parsed.skills.length} relevant skills</p><p className="mt-1 text-slate-400">{parsed.skills.join(' · ')||'We’ll calibrate your profile through the diagnostic.'}</p></div>}{error&&<p className="mt-3 text-sm text-rose-300">{error}</p>}</section><button onClick={continuePath} disabled={busy} className="btn-primary mt-6 w-full sm:w-auto">{busy?'Saving profile…':'Continue to diagnostic'} <Sparkles size={17}/></button></div></main>}
