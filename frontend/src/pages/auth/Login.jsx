import { ArrowRight, GraduationCap } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { login } from '../../services/authApi'
import { useAuthStore } from '../../store/authStore'
export default function Login(){const [form,setForm]=useState({email:'demo@edupath.dev',password:'demopass'}),[error,setError]=useState(''),[busy,setBusy]=useState(false); const save=useAuthStore(s=>s.login),nav=useNavigate(); const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{save(await login(form));nav('/dashboard')}catch(err){setError(err.response?.data?.detail||'Could not sign in. Register a new account instead.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top_right,_#172c4d,_#07111f_55%)] p-5"><section className="grid w-full max-w-4xl overflow-hidden rounded-3xl border border-line bg-slate-950/50 shadow-2xl md:grid-cols-[1.1fr_.9fr]"><div className="hidden bg-sky-400 p-10 text-slate-950 md:block"><GraduationCap size={38}/><p className="mt-24 text-sm font-bold uppercase tracking-widest">Personalized learning</p><h1 className="mt-3 text-4xl font-black leading-tight">Find the shortest route from skills to impact.</h1><p className="mt-5 max-w-sm text-slate-800">Diagnostics that understand confidence, an adaptive roadmap, and an AI mentor in your corner.</p></div><div className="p-7 sm:p-10"><p className="eyebrow">Welcome back</p><h2 className="mt-2 text-3xl font-bold">Continue your path</h2><form onSubmit={submit} className="mt-7 space-y-4"><input className="field" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email" required/><input className="field" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Password" required/>{error&&<p className="text-sm text-rose-300">{error}</p>}<button className="btn-primary w-full" disabled={busy}>{busy?'Signing in…':<>Sign in <ArrowRight size={17}/></>}</button></form><p className="mt-6 text-center text-sm text-slate-400">New to EduPath? <Link className="font-semibold text-sky-300" to="/register">Create an account</Link></p></div></section></main>}
