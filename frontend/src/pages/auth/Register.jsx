import { ArrowRight, GraduationCap } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { register } from '../../services/authApi'
import { useAuthStore } from '../../store/authStore'
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const save=useAuthStore(s=>s.login),nav=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{save(await register(form));nav('/onboarding')}catch(err){setError(err.response?.data?.detail||'Could not create your account')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top_right,_#172c4d,_#07111f_55%)] p-5"><section className="w-full max-w-md rounded-3xl border border-line bg-slate-950/60 p-8 shadow-2xl"><div className="grid h-10 w-10 place-items-center rounded-xl bg-sky-400 text-slate-950"><GraduationCap/></div><p className="eyebrow mt-6">Start with insight</p><h1 className="mt-2 text-3xl font-bold">Build your EduPath</h1><p className="mt-2 text-sm text-slate-400">It takes less than two minutes to personalize.</p><form onSubmit={submit} className="mt-7 space-y-4"><input className="field" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Your name" required/><input className="field" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email address" required/><input className="field" type="password" minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Password (6+ characters)" required/>{error&&<p className="text-sm text-rose-300">{error}</p>}<button className="btn-primary w-full" disabled={busy}>{busy?'Creating…':<>Create account <ArrowRight size={17}/></>}</button></form><p className="mt-6 text-center text-sm text-slate-400">Already have an account? <Link className="font-semibold text-sky-300" to="/login">Sign in</Link></p></section></main>}
